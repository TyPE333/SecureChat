# --- Core Frameworks ---
fastapi==0.111.0
uvicorn[standard]==0.30.0

# --- gRPC ---
grpcio==1.62.0
grpcio-tools==1.62.0

# --- Async utils ---
aiofiles==23.2.1
python-multipart==0.0.9

# --- Data Models / Validation ---
pydantic==2.6.4

# --- Cryptography ---
cryptography==42.0.5

# --- Metrics (optional for v1) ---
prometheus-client==0.19.0

# --- Machine Learning (PyTorch + CPU/GPU builds) ---
# NOTE: DO NOT put torch here — it depends on CUDA version.
# Instead install separately after creating the environment:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# --- Vector DB (for RAG) ---
faiss-cpu==1.7.4
qdrant-client==1.7.3

# --- Utilities ---
pyyaml==6.0.1
